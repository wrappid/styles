import { createContext } from "react";

export const AppStylesContext = createContext({});
/**
 * CoreThemesContext holds all the themes provided by application
 */
export const StylesThemesContext = createContext({});
export const ThemeContext = createContext({});
export const UserThemeContext = createContext(null);
